name: alpha_taxi
description: An application for navigation

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.3
  provider: ^3.2.0
  flutter_launcher_icons: ^0.7.5
  shared_preferences: ^0.5.7+3
  hexcolor: ^1.0.2
  flutter_fadein: ^1.1.1
  simple_animations: ^1.3.4
  page_transition: ^1.1.5
  fluttertoast: ^5.0.1
  pinput: ^0.2.0
  mdi: ^3.0.0
  dio: ^3.0.9
  auto_size_text: ^2.1.0
  permission_handler: ^5.0.1+1
  geolocator: ^5.3.2+2
  google_maps_flutter: ^0.5.28+1
  flutter_statusbarcolor: ^0.2.3
  firebase_auth: ^0.16.1
dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/


  fonts:
    - family: OpenSans
      fonts:
        - asset: fonts/opensans/OpenSansBold.ttf
          weight: 700
        - asset: fonts/opensans/OpenSansSemiBold.ttf
          weight: 300
        - asset: fonts/opensans/OpenSansRegular.ttf
          weight: 200

    - family: CircularStd
      fonts:
        - asset: fonts/circular-std/CircularStd-Bold.ttf
          weight: 700
        - asset: fonts/circular-std/CircularStd-Medium.ttf
          weight: 300
        - asset: fonts/circular-std/CircularStd-Book.ttf
          weight: 200

    - family: Montserrat
      fonts:
        - asset: fonts/montserrat/Montserrat_Bold.otf
          weight: 700
        - asset: fonts/montserrat/Montserrat_Medium.otf
          weight: 300
        - asset: fonts/montserrat/Montserrat_Regular.otf
          weight: 200

analyzer:
  strong-mode: true
  language:
    enableSuperMixins: true